name: checks

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  page:
    name: page budget and assets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check assets, head tags and weight budget
        run: python3 tools/check.py
